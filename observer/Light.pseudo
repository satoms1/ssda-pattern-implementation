public class Light{
	private String color;

	public Light(String color){
		this.color = color;
	}

	/** CHANGE: turnOn() is now the observer response method.
	Reason: Button calls this when notifying registered Lights. **/
	public void turnOn() {
		print(color + " light turned on");
	}
}	
