public SomeOtherClass{

	public main() {

		/** CHANGE: Access same singleton Logger instance.
		Reason: All classes must share one Logger object. **/
		Logger l = Logger.getInstance();

		l.info("Hello from some Other Class!");
	}
}
