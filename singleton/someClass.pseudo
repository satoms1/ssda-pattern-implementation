public SomeClass{

	public main(){

		/** CHANGE: Access singleton Logger instance.
		Reason: Logger objects may not be created directly. **/
		Logger l = Logger.getInstance();

		l.info("Hello World");
	}
}
